import type { Metadata } from "next";
import localFont from "next/font/local";
import "./globals.css";

const geistSans = localFont({
  src: "./fonts/GeistVF.woff",
  variable: "--font-geist-sans",
  weight: "100 900",
});
const geistMono = localFont({
  src: "./fonts/GeistMonoVF.woff",
  variable: "--font-geist-mono",
  weight: "100 900",
});

export const metadata: Metadata = {
  title: "PricingAudit - Compare Your Pricing to Competitors in 60 Seconds",
  description: "Analyze competitor pricing pages and generate comparison reports. Get strategic insights on your SaaS pricing strategy.",
  openGraph: {
    title: "PricingAudit - Competitor Pricing Analysis Tool",
    description: "Compare your pricing to competitors and get strategic insights in 60 seconds.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
